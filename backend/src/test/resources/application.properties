spring.mongodb.embedded.version=3.0.0
spring.mvc.pathmatch.matching-strategy=ant_path_matcher
app.jwt.secret=test-secret

app.oauth.clientId=${GITHUB_CLIENT_ID}
app.oauth.clientSecret=${GITHUB_CLIENT_SECRET}